---
import { Image } from "astro:assets";

const {slug ,image, name } = Astro.props
---
<a href={`/blog/${slug}`}>
<h2>{name}</h2>
<Image src={image.data.attributes.url} alt={image.data.attributes.alternativeText} width="200" height="200">
</a>